server:
  port: 8763

spring:
  application:
    name: users-service

security:
  oauth2:
    resource:
      user-info-uri: http://localhost:9999/user
      token-info-uri: http://localhost:9999/oauth/token
    client:
      client-id: client
      client-secret: secret
